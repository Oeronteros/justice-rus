# Установка ngrok на Windows

## Способ 1: Через Chocolatey (рекомендуется)

```powershell
choco install ngrok
```

После установки ngrok будет доступен из любой директории.

## Способ 2: Ручная установка

1. **Скачайте ngrok:**
   - Перейдите на https://ngrok.com/download
   - Выберите "Windows" и скачайте ZIP файл

2. **Распакуйте:**
   - Распакуйте `ngrok.exe` в удобную папку, например `C:\ngrok\` или `D:\Desktop\ngrok\`

3. **Добавьте в PATH (опционально, но рекомендуется):**
   
   **Через PowerShell (от имени администратора):**
   ```powershell
   # Добавить в PATH для текущего пользователя
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\ngrok", "User")
   ```
   
   Или через GUI:
   - Нажмите `Win + R`, введите `sysdm.cpl`, нажмите Enter
   - Вкладка "Дополнительно" → "Переменные среды"
   - В "Переменные пользователя" найдите `Path` → "Изменить"
   - "Создать" → добавьте путь к папке с ngrok (например: `C:\ngrok`)
   - "OK" → перезапустите PowerShell/Terminal

4. **Проверьте установку:**
   ```powershell
   ngrok version
   ```

## Способ 3: Использование без установки

Если не хотите устанавливать, просто используйте полный путь:

```powershell
# Если ngrok.exe находится в D:\Desktop\ngrok\
& "D:\Desktop\ngrok\ngrok.exe" version
```

## Настройка авторизации

После установки получите authtoken на https://dashboard.ngrok.com/get-started/your-authtoken

Затем выполните:

```powershell
# Если ngrok в PATH:
ngrok config add-authtoken YOUR_AUTHTOKEN

# Или с полным путем:
& "C:\ngrok\ngrok.exe" config add-authtoken YOUR_AUTHTOKEN
```

## Запуск туннеля

```powershell
# Если ngrok в PATH:
ngrok http 3001

# Или с полным путем:
& "C:\ngrok\ngrok.exe" http 3001
```

Вы получите URL вида: `https://abc123.ngrok-free.app`

## Использование с Vercel

1. Скопируйте URL из ngrok (например: `https://abc123.ngrok-free.app`)
2. Vercel Dashboard → Ваш проект → Settings → Environment Variables
3. Добавьте: `DISCORD_BOT_API_URL=https://abc123.ngrok-free.app`
4. Передеплойте проект

## Примечание

⚠️ **Бесплатный план ngrok:**
- URL меняется при каждом перезапуске
- Ограничения по трафику
- Может требовать подтверждение при первом посещении

💡 **Рекомендация:** Используйте Cloudflare Tunnel (см. `docs/cloudflare-tunnel-quickstart.md`) - он бесплатный, стабильный и без ограничений.


