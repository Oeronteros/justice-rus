# Решения для деплоя: Vercel (сайт) + Локальный Discord бот

## Проблема

- **Сайт на Vercel** (в облаке) → не может подключиться к `localhost:3001`
- **Discord бот на локальном компьютере** → недоступен из интернета

## Решения

### 1. 🚀 Туннелирование (ngrok, Cloudflare Tunnel) - Быстрое решение

#### Вариант A: ngrok (самый простой)

1. **Установите ngrok:**
```bash
# Windows: скачайте с https://ngrok.com/download
# Или через chocolatey:
choco install ngrok

# macOS:
brew install ngrok

# Linux:
wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.tgz
tar -xzf ngrok-v3-stable-linux-amd64.tgz
sudo mv ngrok /usr/local/bin
```

2. **Зарегистрируйтесь на ngrok.com** и получите токен

3. **Настройте ngrok:**
```bash
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

4. **Запустите туннель для Discord бота:**
```bash
ngrok http 3001
```

5. **Скопируйте URL** (например: `https://abc123.ngrok.io`)

6. **В Vercel Dashboard** добавьте переменную окружения:
```
DISCORD_BOT_API_URL=https://abc123.ngrok.io
```

**Плюсы:**
- ✅ Быстро настроить
- ✅ Бесплатный план доступен
- ✅ Работает сразу

**Минусы:**
- ❌ URL меняется при каждом перезапуске (на бесплатном плане)
- ❌ Может быть медленнее
- ❌ Ограничения на бесплатном плане

#### Вариант B: Cloudflare Tunnel (бесплатно, стабильно)

1. **Установите cloudflared:**
```bash
# Windows: скачайте с https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/install-and-setup/installation/
# Или через chocolatey:
choco install cloudflared

# macOS:
brew install cloudflared

# Linux:
wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64
chmod +x cloudflared-linux-amd64
sudo mv cloudflared-linux-amd64 /usr/local/bin/cloudflared
```

2. **Авторизуйтесь:**
```bash
cloudflared tunnel login
```

3. **Создайте туннель:**
```bash
cloudflared tunnel create justice-bot
```

4. **Создайте конфиг** `~/.cloudflared/config.yml`:
```yaml
tunnel: <tunnel-id>
credentials-file: ~/.cloudflared/<tunnel-id>.json

ingress:
  - hostname: your-bot.yourdomain.com  # Ваш домен или используйте *.trycloudflare.com
    service: http://localhost:3001
  - service: http_status:404
```

5. **Запустите туннель:**
```bash
cloudflared tunnel run justice-bot
```

6. **В Vercel** используйте URL туннеля

**Плюсы:**
- ✅ Полностью бесплатно
- ✅ Стабильный URL (если используете свой домен)
- ✅ Быстро и надежно

### 2. 🌐 Деплой Discord бота на сервер (Рекомендуется)

#### Вариант A: Railway (простой, бесплатный план)

1. **Создайте аккаунт на Railway.app**

2. **Подключите GitHub репозиторий** с Discord ботом

3. **Railway автоматически определит** и запустит бота

4. **Получите URL** (например: `https://your-bot.up.railway.app`)

5. **В Vercel** добавьте:
```
DISCORD_BOT_API_URL=https://your-bot.up.railway.app
```

**Плюсы:**
- ✅ Бесплатный план (500 часов/месяц)
- ✅ Автоматический деплой
- ✅ Стабильный URL
- ✅ Просто настроить

#### Вариант B: Render (бесплатный план)

1. **Создайте аккаунт на Render.com**

2. **Создайте новый Web Service**

3. **Подключите GitHub репозиторий**

4. **Настройки:**
   - Build Command: `npm install`
   - Start Command: `node server.js` (или ваш стартовый скрипт)
   - Port: 3001

5. **Получите URL** (например: `https://your-bot.onrender.com`)

**Плюсы:**
- ✅ Бесплатный план
- ✅ Автоматический деплой
- ✅ Стабильный URL

**Минусы:**
- ❌ Может "засыпать" после неактивности (на бесплатном плане)

#### Вариант C: VPS (Vultr, DigitalOcean, Hetzner)

1. **Создайте VPS** (самый дешевый ~$5/месяц)

2. **Установите Node.js:**
```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

3. **Клонируйте репозиторий:**
```bash
git clone your-repo.git
cd your-repo
npm install
```

4. **Используйте PM2 для запуска:**
```bash
npm install -g pm2
pm2 start server.js --name discord-bot
pm2 save
pm2 startup  # Автозапуск при перезагрузке
```

5. **Настройте Nginx** (опционально):
```nginx
server {
    listen 80;
    server_name your-bot.yourdomain.com;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Плюсы:**
- ✅ Полный контроль
- ✅ Стабильность
- ✅ Нет ограничений

**Минусы:**
- ❌ Нужно настраивать самому
- ❌ Платно (~$5-10/месяц)

### 3. 🔄 Гибридное решение: Webhook + Queue

Если бот должен оставаться локальным, можно использовать очередь:

1. **Используйте Redis/RabbitMQ** для очереди сообщений
2. **Vercel отправляет запросы** в очередь
3. **Локальный бот обрабатывает** очередь и возвращает результат

Это сложнее, но позволяет оставить бота локальным.

## Рекомендация

**Для начала:** Используйте **ngrok** или **Cloudflare Tunnel** - быстро и бесплатно.

**Для продакшена:** Задеплойте бота на **Railway** или **Render** - стабильно и надежно.

## Пример настройки для ngrok

### В Discord боте (локально):

```javascript
// server.js
const express = require('express');
const app = express();
app.use(express.json());

// Ваши endpoints...
app.get('/api/registrations', async (req, res) => {
  // Получаем данные из БД
  const data = await getRegistrations();
  res.json(data);
});

const PORT = 3001;
app.listen(PORT, () => {
  console.log(`Discord Bot API running on port ${PORT}`);
  console.log(`Ngrok tunnel: https://your-tunnel.ngrok.io`);
});
```

### Запуск:

1. **Терминал 1:** Запустите Discord бота
```bash
node server.js
```

2. **Терминал 2:** Запустите ngrok
```bash
ngrok http 3001
```

3. **Скопируйте URL** из ngrok (например: `https://abc123.ngrok-free.app`)

4. **В Vercel Dashboard:**
   - Settings → Environment Variables
   - Добавьте: `DISCORD_BOT_API_URL=https://abc123.ngrok-free.app`

5. **Передеплойте** сайт на Vercel

## Проверка

После настройки проверьте:

```bash
# Проверьте, что бот доступен
curl https://your-bot-url.ngrok.io/api/registrations

# Должен вернуть JSON с данными
```

## Важные замечания

⚠️ **ngrok бесплатный план:**
- URL меняется при каждом перезапуске
- Используйте ngrok с фиксированным доменом (платно) или обновляйте переменную в Vercel

⚠️ **Безопасность:**
- Используйте аутентификацию в Discord боте
- Ограничьте доступ по IP (если возможно)
- Используйте HTTPS

⚠️ **Производительность:**
- Туннели могут добавлять задержку
- Для продакшена лучше задеплоить бота на сервер

